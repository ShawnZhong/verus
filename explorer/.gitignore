# Cargo build output
/target/

# Build output (regenerate: `make` or `make release`)
/dist/
/verus-explorer.tar.gz

# Toolchain + Z3 source caches (re-bootstrapped by scripts/build-z3-wasm.sh)
/third_party/
